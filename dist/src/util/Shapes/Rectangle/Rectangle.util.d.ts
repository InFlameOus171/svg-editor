import { Coordinates, BoundaryCoordinates } from '../../../types/types';
export declare const getRectangleBoundaries: (startingCorner: Coordinates, width: number, height: number) => BoundaryCoordinates;
